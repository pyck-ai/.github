# CLAUDE.md - Project Context

## Repository Structure
- This is a GitHub template repository named `.github`
- The repository itself is named `.github` (located at `/home/mmack/workspace/pyck/current/.github/`)
- Inside this repository, there's a `.github` folder containing the actual template files
- Path structure: `.github/.github/` contains all template files:
  - PULL_REQUEST_TEMPLATE.md
  - ISSUE_TEMPLATE/ (contains feature.yml, defect.yml, task.yml, business-goal.yml)
  - CODE_OF_CONDUCT.md
  - SECURITY.md

## Important Notes
- When working with this repo, remember the confusing but correct path: `.github/.github/`
- All template files are meant to be used by other repositories when they use this as a template